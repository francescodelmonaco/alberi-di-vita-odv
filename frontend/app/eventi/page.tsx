import Hero from "../components/hero";

export default function Eventi() {
    return (
        <div className="min-h-screen">
            <Hero
                pageName="Eventi"
                pageDescription="Scopri gli eventi che abbiamo organizzato per la comunità."
            />
        </div>
    )
}
